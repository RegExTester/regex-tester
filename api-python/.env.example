# Port the Uvicorn server listens on.
PORT=5200

# "development" enables permissive localhost CORS; any other value (e.g. "production") does not.
ENVIRONMENT=production

# Comma-separated list of additional CORS origins to allow, beyond https://regextester.github.io.
ALLOW_CORS=

# Optional Cosmos DB telemetry settings. api-python's telemetry_service is currently a no-op stub
# (see TASK-08), so these are unused for now but reserved for parity with the other backends.
COSMOS_CONNECTION_STRING=
COSMOS_DATABASE=regex-tester-db
COSMOS_CONTAINER=telemetry
