{
  "openapi": "3.1.1",
  "info": {
    "title": "RegEx Tester API",
    "description": "REST API for testing .NET regular expressions. Accepts a pattern, input text, and option flags; returns all matches with their groups and captures. Supports URL-based sharing via Base64Url-encoded query parameters.",
    "contact": {
      "name": "RegEx Tester",
      "url": "https://regextester.github.io/"
    },
    "version": "v1"
  },
  "servers": [
    {
      "url": "https://regex-tester-api-dotnet.azurewebsites.net",
      "description": "Production (Azure App Service)"
    },
    {
      "url": "http://localhost:5000",
      "description": "Local development"
    }
  ],
  "paths": {
    "/": {
      "get": {
        "tags": [
          "Home"
        ],
        "summary": "Redirect to the Angular frontend.",
        "responses": {
          "302": {
            "description": "HTTP 302 redirect to `https://regextester.github.io/`.",
            "content": {
              "application/json": {}
            }
          }
        }
      }
    },
    "/api/version": {
      "get": {
        "tags": [
          "Home"
        ],
        "summary": "Return runtime version information for the host.",
        "description": "Response is cached for 24 hours. Includes a `debug` flag when built in DEBUG configuration.",
        "responses": {
          "200": {
            "description": "Version information.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "os": {
                      "type": "string",
                      "description": "Operating system description.",
                      "example": "Microsoft Windows 10.0.26200"
                    },
                    "framework": {
                      "type": "string",
                      "description": ".NET framework description.",
                      "example": ".NET 10.0.5"
                    },
                    "debug": {
                      "type": "integer",
                      "description": "Present with value `1` only in DEBUG builds.",
                      "example": 1
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/regex": {
      "post": {
        "tags": [
          "RegEx"
        ],
        "summary": "Run a regular expression and return all matches.",
        "description": "Applies the provided regex pattern to the input text using the specified option flags.\nAll string fields are Base64Url-encoded by the Angular frontend before submission but\nthe API itself accepts plain UTF-8 JSON strings.\n\n**Timeout:** The regex engine enforces a 15-second match timeout; the HTTP request\nhas a 5-second middleware timeout. If either is exceeded an error message is returned\nin the `error` field rather than throwing an HTTP error.",
        "requestBody": {
          "description": "Pattern, text, optional replacement string, and regex option flags.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Input"
              },
              "example": {
                "pattern": "(\\w+)",
                "text": "Hello World",
                "replace": null,
                "options": 1
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Regex executed successfully; inspect `error` field for pattern errors.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegexResult"
                },
                "example": {
                  "error": null,
                  "replace": null,
                  "matches": [
                    {
                      "name": "0",
                      "index": 0,
                      "length": 5,
                      "value": "Hello",
                      "groups": [
                        {
                          "name": "0",
                          "index": 0,
                          "length": 5,
                          "value": "Hello"
                        },
                        {
                          "name": "1",
                          "index": 0,
                          "length": 5,
                          "value": "Hello"
                        }
                      ]
                    }
                  ]
                }
              }
            }
          },
          "400": {
            "description": "Request body failed model validation (e.g. pattern > 512 chars).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Input": {
        "type": "object",
        "description": "Request body for the regex matching endpoint.",
        "properties": {
          "options": {
            "description": "Bitwise combination of RegExTesterOptions flags. Use `0` for no options. The custom `ShowCaptures` flag (32768) causes capture collections to be included in each match and group; it is stripped before being passed to the .NET `RegexOptions` enum.",
            "$ref": "#/components/schemas/RegExTesterOptions"
          },
          "pattern": {
            "maxLength": 512,
            "minLength": 0,
            "type": ["null", "string"],
            "description": "The regular expression pattern to evaluate. Maximum 512 characters."
          },
          "text": {
            "maxLength": 1024,
            "minLength": 0,
            "type": ["null", "string"],
            "description": "The input text to search. Maximum 1024 characters."
          },
          "replace": {
            "maxLength": 1024,
            "minLength": 0,
            "type": ["null", "string"],
            "description": "Optional replacement string. When supplied, the response will include a `replace` field containing the result of calling `Regex.Replace(text, replace)`. Maximum 1024 characters."
          }
        }
      },
      "RegExTesterOptions": {
        "type": "integer",
        "format": "int32",
        "description": "Bitwise flags controlling regex matching behaviour. Values 0–1024 map directly to .NET `RegexOptions`. `ShowCaptures` (32768) is a custom flag handled by the API before forwarding to .NET.",
        "enum": [0, 1, 2, 4, 8, 16, 32, 64, 256, 512, 1024, 32768],
        "x-enumNames": [
          "None",
          "IgnoreCase",
          "Multiline",
          "ExplicitCapture",
          "Compiled",
          "Singleline",
          "IgnorePatternWhitespace",
          "RightToLeft",
          "ECMAScript",
          "CultureInvariant",
          "NonBacktracking",
          "ShowCaptures"
        ],
        "x-enumDescriptions": {
          "0":     "No options. Default behaviour.",
          "1":     "Case-insensitive matching.",
          "2":     "^ and $ match start/end of each line.",
          "4":     "Only explicitly named or numbered groups are captured.",
          "8":     "Compile the expression to an assembly for faster repeated use.",
          "16":    ". matches every character including \\n.",
          "32":    "Unescaped whitespace in the pattern is ignored; # starts a comment.",
          "64":    "Search proceeds right-to-left instead of left-to-right.",
          "256":   "ECMAScript-compliant behaviour (combine with IgnoreCase/Multiline only).",
          "512":   "Cultural invariant matching regardless of current locale.",
          "1024":  "Use a non-backtracking (linear-time) matching engine.",
          "32768": "Custom flag: include capture collections in each match and group."
        }
      },
      "RegexResult": {
        "type": "object",
        "description": "Top-level result returned by the regex matching endpoint.",
        "properties": {
          "error": {
            "type": ["null", "string"],
            "description": "Error message when the pattern is invalid or the match times out; `null` on success."
          },
          "replace": {
            "type": ["null", "string"],
            "description": "String produced by applying the `replace` pattern to the input text. `null` when no replace pattern was supplied."
          },
          "matches": {
            "type": ["null", "array"],
            "items": {
              "$ref": "#/components/schemas/MatchResult"
            },
            "description": "All matches found in the input text. Empty array when there are no matches."
          }
        }
      },
      "MatchResult": {
        "type": "object",
        "description": "A single regex match within the input text.",
        "properties": {
          "name": {
            "type": ["null", "string"],
            "description": "Match name (always `\"0\"` for the whole-match group)."
          },
          "index": {
            "type": "integer",
            "format": "int32",
            "description": "Zero-based character offset where the match starts."
          },
          "length": {
            "type": "integer",
            "format": "int32",
            "description": "Length of the matched substring in characters."
          },
          "value": {
            "type": ["null", "string"],
            "description": "The matched substring."
          },
          "groups": {
            "type": ["null", "array"],
            "items": {
              "$ref": "#/components/schemas/GroupResult"
            },
            "description": "Named and numbered capturing groups within this match."
          },
          "captures": {
            "type": ["null", "array"],
            "items": {
              "$ref": "#/components/schemas/CaptureResult"
            },
            "description": "All captures of the whole match. Only populated when the `ShowCaptures` flag (32768) is set in `options`."
          }
        }
      },
      "GroupResult": {
        "type": "object",
        "description": "A single capturing group within a match.",
        "properties": {
          "name": {
            "type": ["null", "string"],
            "description": "Group name or number (e.g. `\"1\"`, `\"word\"`)."
          },
          "index": {
            "type": "integer",
            "format": "int32",
            "description": "Zero-based character offset where the group match starts."
          },
          "length": {
            "type": "integer",
            "format": "int32",
            "description": "Length of the group match in characters."
          },
          "value": {
            "type": ["null", "string"],
            "description": "The substring matched by this group."
          },
          "captures": {
            "type": ["null", "array"],
            "items": {
              "$ref": "#/components/schemas/CaptureResult"
            },
            "description": "All individual captures of this group. Only populated when the `ShowCaptures` flag (32768) is set in `options`."
          }
        }
      },
      "CaptureResult": {
        "type": "object",
        "description": "An individual capture produced by a group that matches multiple times.",
        "properties": {
          "index": {
            "type": "integer",
            "format": "int32",
            "description": "Zero-based character offset where this capture starts."
          },
          "length": {
            "type": "integer",
            "format": "int32",
            "description": "Length of this capture in characters."
          },
          "value": {
            "type": ["null", "string"],
            "description": "The captured substring."
          }
        }
      },
      "ProblemDetails": {
        "type": "object",
        "properties": {
          "type": { "type": ["null", "string"] },
          "title": { "type": ["null", "string"] },
          "status": { "type": ["null", "integer"], "format": "int32" },
          "detail": { "type": ["null", "string"] },
          "instance": { "type": ["null", "string"] }
        }
      }
    }
  },
  "tags": [
    {
      "name": "Home",
      "description": "Utility endpoints for navigation and version information."
    },
    {
      "name": "RegEx",
      "description": "Executes .NET regular expressions against supplied input text."
    }
  ]
}
